# Инструкция по загрузке на GitHub

## Репозиторий уже инициализирован! ✅

Git репозиторий создан и первый коммит сделан.

## Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите **"+"** в правом верхнем углу → **"New repository"**
4. Заполните форму:
   - **Repository name:** `blackwood.uz` (или другое название)
   - **Description:** "Сайт-визитка BLACKWOOD - Мебель на заказ в Ташкенте"
   - **Visibility:** Public (или Private, если хотите закрытый репозиторий)
   - **НЕ** ставьте галочки на "Initialize with README" (у нас уже есть файлы)
5. Нажмите **"Create repository"**

## Шаг 2: Подключите локальный репозиторий к GitHub

GitHub покажет вам команды. Выполните их в терминале:

```bash
# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/blackwood.uz.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

### Или через SSH (если настроен):

```bash
git remote add origin git@github.com:YOUR_USERNAME/blackwood.uz.git
git branch -M main
git push -u origin main
```

## Шаг 3: Проверка

После выполнения команд:
1. Обновите страницу репозитория на GitHub
2. Все файлы должны быть загружены
3. Репозиторий готов!

## Дальнейшая работа

### Обновление сайта:

```bash
# После изменений файлов:
git add .
git commit -m "Описание изменений"
git push
```

### Если используете Netlify:

1. Подключите GitHub репозиторий к Netlify
2. Netlify будет автоматически обновлять сайт при каждом `git push`
3. Очень удобно! 🚀

## Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log

# Посмотреть изменения
git diff

# Отменить изменения в файле
git checkout -- имя-файла
```

## Готово!

Теперь ваш код на GitHub и вы можете:
- ✅ Делиться кодом
- ✅ Отслеживать изменения
- ✅ Автоматически деплоить на Netlify
- ✅ Работать в команде

**Следующий шаг:** Создайте репозиторий на GitHub и выполните команды из Шага 2!
